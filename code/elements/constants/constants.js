const width = window.innerWidth / 2;
const height = window.innerHeight;

const leftBorder = 0;
const rightBorder = width;
const upBorder = 0;
const downBorder = height;

export { width, height, leftBorder, rightBorder, upBorder, downBorder };
